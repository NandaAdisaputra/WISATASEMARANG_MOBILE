<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin untuk akses internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WISATASEMARANG"
        tools:targetApi="31">

        <!-- Pastikan file data_extraction_rules.xml ada atau hapus jika tidak digunakan -->
        <meta-data
            android:name="android.content.APP_DATA_EXTRACTION_RULES"
            android:resource="@xml/data_extraction_rules"
            tools:ignore="MissingResource"/>

        <!-- Aktivitas Tambah Wisata -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"/>

        <!-- Aktivitas Utama -->
        <activity
            android:name=".ui.TambahWisataActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.nandaadisaputra.wisatasemarang.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
